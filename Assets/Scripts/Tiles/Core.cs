namespace Tiles
{
    public enum TileStyle { Grass, Forest, Mountain, Water, Coast, Field }
    public enum ResourceType { None, Wood, Stone, Iron, Seed}
    public enum ConstructionType { None, Hut, Mill, Mine, Well }
}